using UnityEngine;
using System.Collections;
/* This Class deals with
 * Settings that are non dependant on database and maybe cached directly into the pokemonPrefab
 * EG. AudioClips,Sprite,Custom decalsetc
 */
public class PokeSettings : MonoBehaviour
{


    
 
    public GameObject AOEDecal;
    public GameObject thunderBoltPrefab;// is the AOE cursor effect using unitys projection system 
    // Use this for initialization
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {

    }
}
